import os

class Finder:

    def pathFind(self, inputGraph, Goal):
        graph = inputGraph
        visited = []
        start = graph.start
        end = graph.end
        currentNode = start
        currentMax = 0
        
        while(visited[len(visited)] !=  end):
            for i in graph[currentNode]
                if( i[0] > currentMax && i not in visited):
                    currentMax = i



    
    
            
